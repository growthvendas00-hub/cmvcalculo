node_modules/
.env
.DS_Store
*.log

# Credenciais locais (senha/segredo de sessão) — nunca versionar
data/settings.json

# Backups automáticos locais (cópias do banco) — não versionar
data/backups/
*.tmp
